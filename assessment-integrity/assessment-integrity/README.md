
  # Create Page with Typescript

  This is a code bundle for Create Page with Typescript. The original project is available at https://www.figma.com/design/ZDSIgacsB1SmmBBCRihzKp/Create-Page-with-Typescript.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  